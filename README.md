# RoastDiscord

Create an `.env` file with `DISCORD_TOKEN_ROAST=your_token`.

Uses [koboldcpp](https://github.com/LostRuins/koboldcpp) as backend on localhost on port `5001`.

Feel free to change the `roast.txt` to change persona.

### Launch

`docker compose up --build -d`
