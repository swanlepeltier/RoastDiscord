services:
  roast-bot:
    build: .
    container_name: roast-bot
    network_mode: "host"
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN_ROAST}
    restart: unless-stopped
    volumes:
      - ./main.py:/app/main.py
      - ./roast.txt:/app/roast.txt
